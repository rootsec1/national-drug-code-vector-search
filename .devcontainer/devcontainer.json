{
    "name": "Python 3",
    "build": {
        "dockerfile": "../Dockerfile",
        "context": "..",
        "args": { 
            "VARIANT": "3.10"
        }
    },
    "runArgs": [
        "--network",
        "host"
    ],
    "settings": { 
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-python.python"
    ]
}
